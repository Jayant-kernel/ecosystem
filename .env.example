# Copy to .env and fill in. .env is gitignored — never commit it.

# --- AI (Gemini) — ACTIVE ---
# Used by the browser voice tutor (services/geminiService.ts).
API_KEY=

# --- Optional: AWS pipeline (currently parked) ---
# Frontend-safe base URL for the SAM stack.
VITE_API_BASE_URL=

# Server-side only — NEVER prefix these with VITE_.
ELEVENLABS_API_KEY=
ELEVENLABS_VOICE_ID=
BEDROCK_MODEL_ID=anthropic.claude-3-haiku-20240307-v1:0
BEDROCK_REGION=ap-south-1
AWS_REGION=ap-south-1
